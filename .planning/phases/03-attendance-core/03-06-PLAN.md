---
phase: 03-attendance-core
plan: "06"
type: execute
wave: 4
depends_on:
  - "03-01"
  - "03-02"
  - "03-03"
  - "03-04"
  - "03-05"
files_modified: []
autonomous: false
requirements: []

must_haves:
  truths:
    - "Employee can check in with camera photo and the record appears in their history"
    - "Late check-in requires and stores a reason"
    - "Check-out works and missing checkout is auto-marked"
    - "Admin can see all company records with inline photos"
    - "IP enforcement works as configured"
---

<objective>
Human verification of the full Phase 3 attendance core feature end-to-end.

Purpose: Confirms all automated work from Plans 01-05 is correct and integrated before Phase 4 begins.

Output: Human approval or issue report.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/03-attendance-core/03-01-SUMMARY.md
@.planning/phases/03-attendance-core/03-02-SUMMARY.md
@.planning/phases/03-attendance-core/03-03-SUMMARY.md
@.planning/phases/03-attendance-core/03-04-SUMMARY.md
@.planning/phases/03-attendance-core/03-05-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
Complete Phase 3 — Attendance Core:
- Backend: AttendanceModule with check-in, check-out, history, records endpoints + photo upload URL generation + midnight cron for missing checkouts
- DB: attendance_records table with full schema, RLS policy, Supabase Storage bucket (attendance-photos)
- Frontend: Employee dashboard with camera-based check-in/out, employee history page, admin/manager attendance record view with inline photos
  </what-built>
  <how-to-verify>
Run both servers: `cd backend && npm run start:dev` and `cd frontend && npm run dev`

**Apply SQL migrations first (in Supabase SQL editor):**
1. Run `backend/src/database/migrations/004_attendance_records.sql`
2. Run `backend/src/database/rls/003_attendance_rls.sql`
3. Create Supabase Storage bucket named `attendance-photos` (public: false) via Supabase Dashboard → Storage

**Check-in flow (as employee/manager):**
4. Log in as an employee who has an active shift assigned
5. Visit http://localhost:3000 — confirm you see a large CHECK-IN button (not a table, not a placeholder)
6. Click CHECK-IN — confirm camera activates in the browser (permission dialog appears, then live video preview)
7. Click "Capture Photo" — confirm still frame is captured and shown as thumbnail
8. Click "Submit" — if on-time, record is created; if late, confirm a reason text field appears before Submit becomes active
9. Confirm the button changes to CHECK-OUT after successful check-in
10. Click CHECK-OUT — camera activates again; capture and submit; if early, confirm note field appears

**History page (as employee):**
11. Visit http://localhost:3000/attendance/history
12. Confirm today's record appears with correct status badge (on-time/within-grace/late)
13. Expand the row — confirm check-in photo thumbnail is visible
14. Change the month — confirm records update

**Admin/Manager view:**
15. Log in as Admin and visit http://localhost:3000/admin/attendance
16. Confirm today's records appear for all company employees
17. Click "View" on the employee's record — confirm detail modal shows both check-in and check-out photos inline
18. Confirm employee filter dropdown works (select a specific employee → records filter)

**IP enforcement (optional — if IP allowlist is configured):**
19. Set ip_mode to 'enforce-block' and add a specific IP to ip_allowlist via `PATCH /company/settings`
20. Attempt check-in from a different IP — confirm 403 error message appears in UI

**Missing checkout cron (manual test):**
21. In Supabase SQL editor, manually insert a test attendance_records row with check_in_at set, check_out_at null, work_date = yesterday, missing_checkout = false
22. Call the cron manually or wait for next execution; confirm missing_checkout becomes true
  </how-to-verify>
  <resume-signal>Type "approved" if all steps pass, or describe any issues found</resume-signal>
</task>

</tasks>

<verification>
Human checkpoint — verified by user.
</verification>

<success_criteria>
All 22 verification steps pass. Phase 3 Attendance Core is complete and ready for Phase 4 Admin Adjustments.
</success_criteria>

<output>
After approval, update:
1. `.planning/ROADMAP.md` — Phase 3 plans count updated to 6, status set to Complete
2. `.planning/STATE.md` — Current position updated to Phase 3 complete, Phase 4 ready
3. `.planning/REQUIREMENTS.md` — ATTN-01 through ATTN-13 and EVID-01 through EVID-04 marked complete
4. Create `.planning/phases/03-attendance-core/03-06-SUMMARY.md`
</output>
