---
phase: 05-monitoring-reporting
plan: "05"
type: execute
wave: 3
depends_on: ["05-03", "05-04"]
files_modified: []
autonomous: false
requirements: [MNGR-01, MNGR-02, MNGR-03, MNGR-04, MNGR-05, MNGR-06, EXEC-01, EXEC-02, EXEC-03, EXEC-04, EXEC-05, RPTS-01, RPTS-02, RPTS-03]

must_haves:
  truths:
    - "Manager logs in and views /admin/attendance — only sees their assigned employees, not the full company"
    - "Manager sees team summary KPI cards at top of attendance page (Total Records, Late, Punctuality Rate)"
    - "Executive logs in and views /executive — sees company-wide attendance rate, late ranking, and monthly breakdown; no edit buttons exist"
    - "Admin views /admin/reports — sees stats cards and records table; clicks Export CSV — file downloads to browser"
  artifacts:
    - path: "frontend/src/app/(app)/executive/page.tsx"
      provides: "Executive dashboard — renders without error"
    - path: "frontend/src/app/(app)/admin/reports/page.tsx"
      provides: "Reports page — renders without error, CSV export works"
  key_links:
    - from: "Manager /admin/attendance page"
      to: "backend GET /attendance/records (scoped)"
      via: "manager sees only their employees via manager_id scoping"
    - from: "/admin/reports Export CSV button"
      to: "GET /attendance/export/csv"
      via: "blob download triggers file save dialog"
---

<objective>
Human verification checkpoint to confirm all Phase 5 user journeys work correctly end-to-end before marking the phase complete.

Purpose: Phase 5 delivers three distinct user roles with different views. Human verification confirms that role-based access, data scoping, and the CSV export all function as specified.

Output: Verified working system — human approves or reports issues for fix.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Start backend and frontend for verification</name>
  <files></files>
  <action>
    Confirm both services start cleanly:
    1. Run `cd /Users/avepoint_admin/Downloads/AttendanceWebApp/backend && npm run build` — confirm zero errors.
    2. Run `cd /Users/avepoint_admin/Downloads/AttendanceWebApp/frontend && npm run build` — confirm zero errors.

    If any TypeScript or build errors appear, fix them before proceeding to the checkpoint.
  </action>
  <verify>
    <automated>cd /Users/avepoint_admin/Downloads/AttendanceWebApp/backend && npx tsc --noEmit 2>&1 | head -10 && cd /Users/avepoint_admin/Downloads/AttendanceWebApp/frontend && npx tsc --noEmit 2>&1 | head -10</automated>
  </verify>
  <done>Both backend and frontend TypeScript compile with zero errors.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Phase 5 complete implementation:
    - Manager-scoped attendance view (/admin/attendance) with team summary KPI card
    - Executive read-only dashboard (/executive) with attendance rate, late ranking, monthly breakdown
    - Admin/Manager reports page (/admin/reports) with stats and CSV export
    - All role-based access controls enforced

    Start services: backend on port 3001, frontend on port 3000.
  </what-built>
  <how-to-verify>
    **Prerequisite:** Backend and frontend running (npm run start:dev / npm run dev).

    **Test 1: Manager view — scoped attendance**
    1. Log in as a Manager account
    2. Navigate to Attendance (or /admin/attendance)
    3. Confirm: records visible are ONLY for employees assigned to this manager (compare with known assignment)
    4. Confirm: team summary card shows Total Records, Late count, Punctuality Rate at top of page
    5. Confirm: employee filter dropdown lists only the manager's assigned employees

    **Test 2: Manager view — record detail (notes + photos)**
    1. While on /admin/attendance as Manager, click any record to open detail modal
    2. Confirm: late reason and/or early note visible if present
    3. Confirm: check-in and check-out photos visible inline

    **Test 3: Manager view — team summary endpoint**
    1. In browser DevTools → Network, observe call to /attendance/reports/team-summary
    2. Confirm: response contains `{ total, late, punctualityRate, monthlyBreakdown }`

    **Test 4: Executive dashboard — stats**
    1. Log out, log in as an Executive account
    2. Navigate to /executive (or click "Executive" nav link)
    3. Confirm: Attendance Rate card shows a percentage
    4. Confirm: Late Frequency Ranking table shows employees and late day counts
    5. Confirm: Daily Breakdown table shows dates with present/late/missing checkout counts
    6. Confirm: NO edit buttons, NO adjust buttons, NO CRUD controls anywhere on the page

    **Test 5: Executive — read-only enforcement**
    1. Confirm executive can navigate to /executive but NOT to /admin/attendance, /admin/users, /admin/shifts (should be redirected to /dashboard)

    **Test 6: Admin reports page**
    1. Log out, log in as Admin
    2. Navigate to /admin/reports (or click "Reports" nav link)
    3. Confirm: Stats cards show Total, On Time, Within Grace, Late, Late Rate
    4. Confirm: Records table shows employee names, check-in times, status badges, late reasons

    **Test 7: CSV export**
    1. On /admin/reports page, click "Export CSV" button
    2. Confirm: browser downloads a file named `attendance-YYYY-MM.csv`
    3. Open the CSV file — confirm it has headers (Date, Employee, Check-in, etc.) and data rows

    **Test 8: Month navigation**
    1. On /admin/reports, click prev/next month arrows
    2. Confirm: stats and records update to reflect selected month

    **Test 9: Nav link visibility**
    1. Log in as Manager — confirm "Reports" link visible in nav, "Executive" link NOT visible
    2. Log in as Executive — confirm "Executive" link visible in nav, admin links NOT visible
  </how-to-verify>
  <resume-signal>Type "approved" to complete Phase 5, or describe any issues found for fixing</resume-signal>
</task>

</tasks>

<verification>
Human approves all 9 test cases above.
</verification>

<success_criteria>
- Manager sees only assigned employees on attendance page — team summary card rendered correctly
- Executive dashboard shows company-wide stats with no edit controls
- Reports page renders monthly stats and allows CSV download
- Nav links correctly shown/hidden per role
- Human verification: all test cases pass
</success_criteria>

<output>
After approval, create `.planning/phases/05-monitoring-reporting/05-05-SUMMARY.md`

Update ROADMAP.md Phase 5 status to Complete.
Update STATE.md: Phase 5 complete, project 100% complete (all 53 v1 requirements delivered).
</output>
