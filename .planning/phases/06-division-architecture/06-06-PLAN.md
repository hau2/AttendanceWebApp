---
phase: 06-division-architecture
plan: "06"
type: execute
wave: 4
depends_on: ["06-04", "06-05"]
files_modified: []
autonomous: false
requirements: [DIVN-01, DIVN-02, DIVN-03, DIVN-04, DIVN-05, DIVN-06, DIVN-07]

must_haves:
  truths:
    - "Admin can create a Division, see it in the list with the assigned Manager's name, edit it, and delete it (empty division only)"
    - "Every employee row in /admin/users shows their Division assignment and Admin can change it"
    - "Manager sees only their Division's employees in /admin/attendance and /admin/reports"
    - "Admin and Executive see all employees across all Divisions in every view"
    - "Attempting to delete a Division that has employees shows a clear error, not a silent failure"
  artifacts:
    - path: "frontend/src/app/(app)/admin/divisions/page.tsx"
      provides: "Division management page accessible at /admin/divisions"
    - path: "backend/src/divisions/divisions.service.ts"
      provides: "Fully working CRUD for divisions"
    - path: "backend/src/attendance/attendance.service.ts"
      provides: "Manager scoped via division membership"
  key_links:
    - from: "Manager login"
      to: "/admin/attendance (records view)"
      via: "listRecords() scoped by division membership"
      pattern: "division-scoped attendance"
---

<objective>
Human E2E verification of Phase 6: Division Architecture. Verify all four success criteria with real browser interaction.

Purpose: Division scoping is a security-critical requirement (DIVN-06). A Manager viewing cross-division data is a data leak. This checkpoint verifies that the backend scope change is correct and that the full Admin workflow for Division management works end-to-end.

Output: User confirms all test cases pass. Phase 6 is marked complete.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Phase 6 Division Architecture — all 5 plans delivered:
    - DB schema: divisions table + users.division_id column (Plan 01)
    - NestJS DivisionsModule: POST/GET/PATCH/DELETE /divisions with employee guard on delete (Plan 02)
    - Manager scope migration: attendance queries use division membership instead of manager_id (Plan 03)
    - Division Management UI at /admin/divisions with Create/Edit/Delete modals (Plan 04)
    - User Management updated with Division column + assignment dropdown + division in Create User form (Plan 05)

    Prerequisites: SQL migrations 007_divisions.sql and 004_divisions_rls.sql must be run in Supabase SQL editor first.
  </what-built>
  <how-to-verify>
    Start both servers:
    - Backend: `cd backend && npm run start:dev`
    - Frontend: `cd frontend && npm run dev` (http://localhost:3000)

    Run migrations in Supabase SQL editor before testing:
    1. Run `backend/src/database/migrations/007_divisions.sql`
    2. Run `backend/src/database/rls/004_divisions_rls.sql`

    **Test Case 1 — Division CRUD (DIVN-01, DIVN-02, DIVN-03, DIVN-04):**
    a. Log in as Admin. Click "Divisions" in the nav.
    b. Verify: /admin/divisions loads with an empty table or existing divisions.
    c. Click "Add Division". Fill in Name: "Engineering". Assign a Manager. Submit.
    d. Verify: "Engineering" appears in the table with the Manager's name shown.
    e. Create a second division: "Sales" with a different Manager (or no manager).
    f. Click "Edit" on Engineering. Change the name to "Engineering Team". Save.
    g. Verify: The row now shows "Engineering Team".
    h. Click "Delete" on a division that has NO employees assigned. Confirm.
    i. Verify: The division disappears from the list.
    j. Skip to Test Case 2 to assign employees to a division, then come back to test:
       - Click "Delete" on a division WITH employees assigned.
       - Verify: An error message appears (e.g., "Cannot delete division: X employee(s) are still assigned to it"). The division remains in the list.

    **Test Case 2 — Division assignment on users (DIVN-05):**
    a. Navigate to /admin/users.
    b. Verify: A "Division" column appears in the user table (shows '—' for users with no division).
    c. For an employee row, find the division dropdown in the Actions column.
    d. Select a Division. Verify: The "Division" column for that employee updates to show the division name.
    e. Click "Add User". Verify: The form includes a "Division" selector.
    f. Create a new employee and assign them to a division.
    g. Verify: The new user appears in the table with the correct Division shown.

    **Test Case 3 — Manager scope: Division-based view (DIVN-06):**
    a. Create/confirm: Division "Engineering" has Manager Alice. Division "Sales" has Manager Bob.
    b. Assign 2 employees to "Engineering" and 2 employees to "Sales" using the /admin/users division dropdowns.
    c. Generate some attendance records for both groups (or use existing ones).
    d. Log in as Alice (Manager of Engineering).
    e. Navigate to /admin/attendance (Records tab).
    f. Verify: Alice only sees attendance records for the 2 Engineering employees — NOT the 2 Sales employees.
    g. Navigate to /admin/reports.
    h. Verify: Alice's team summary and monthly report only reflects Engineering employees.
    i. Log out. Log in as Bob (Manager of Sales).
    j. Navigate to /admin/attendance.
    k. Verify: Bob only sees records for the 2 Sales employees — NOT Engineering employees.

    **Test Case 4 — Admin and Executive see all (DIVN-07):**
    a. Log in as Admin.
    b. Navigate to /admin/attendance.
    c. Verify: Admin sees attendance records for ALL employees across all Divisions.
    d. Log in as Executive.
    e. Navigate to /executive.
    f. Verify: Executive dashboard shows company-wide data (not limited to one Division).

    **Test Case 5 — No regression:**
    a. Log in as any employee. Verify: dashboard and attendance history still work normally.
    b. Log in as Admin. Verify: /admin/shifts page still loads correctly.
  </how-to-verify>
  <resume-signal>Type "approved" if all test cases pass. Describe any failures with the test case letter (e.g., "3f failed — saw Sales records in Engineering manager view").</resume-signal>
</task>

</tasks>

<verification>
Human verification as described above. All 5 test cases must pass before Phase 6 is considered complete.
</verification>

<success_criteria>
- Test Case 1: Full Division CRUD works including blocked delete with clear error
- Test Case 2: Division column visible in user table; assignment dropdown works; Create User form has Division field
- Test Case 3: Manager only sees their Division's employees in all attendance views
- Test Case 4: Admin and Executive see all employees across all Divisions
- Test Case 5: No regressions in existing functionality
</success_criteria>

<output>
After completion, create `.planning/phases/06-division-architecture/06-06-SUMMARY.md`
</output>
