# Plan 13-01: Design System + Auth Pages (Stitch-exact)

---
wave: 1
depends_on: []
files_modified:
  - frontend/tailwind.config.ts
  - frontend/src/app/globals.css
  - frontend/src/app/(auth)/layout.tsx
  - frontend/src/app/(auth)/login/page.tsx
  - frontend/src/app/(auth)/register/page.tsx
autonomous: true
requirements: []
---

## Objective

Match login and register pages pixel-perfect to Stitch HTML. Add primary color `#4848e5`, background `#f6f6f8` to Tailwind config. Add brand logo. Add icon inputs.

## Reference

- `stitch-html/login.html` — exact source
- `stitch-html/register.html` — exact source
- `DESIGN-TOKENS.md` — all values

## Tasks

<task id="1">
### Tailwind Config + Globals

Add to `tailwind.config.ts` extended colors:
```
"primary": "#4848e5",
"background-light": "#f6f6f8",
```

In `globals.css`, add `antialiased` to body if not present. Ensure Geist font is loaded (already is via Next.js).
</task>

<task id="2">
### Auth Layout

Rewrite `(auth)/layout.tsx` to match Stitch login.html structure:
- `bg-[#f6f6f8] min-h-screen flex items-center justify-center`
- Max width container `max-w-[480px]`
- Brand logo: `h-12 w-12 rounded-xl bg-[#4848e5] shadow-lg shadow-[#4848e5]/30 text-white flex items-center justify-center` with Lucide `UserCheck` icon
- Title: `text-[32px] font-bold leading-tight tracking-tight`
- Subtitle: `text-slate-500 text-base`
</task>

<task id="3">
### Login Page (match stitch-html/login.html exactly)

Key classes from Stitch source:
- Card: `bg-white rounded-xl shadow-sm border border-slate-200 p-8 flex flex-col gap-6`
- Heading: `text-xl font-bold` + subtitle `text-slate-500 text-sm`
- Email input: `h-12 pl-11 pr-4 rounded-lg border border-slate-300 text-base placeholder:text-slate-400 focus:ring-2 focus:ring-[#4848e5]/50 focus:border-[#4848e5]` with Mail icon (Lucide) at `absolute left-0 pl-4 text-slate-400`
- Password: same + "Forgot password?" link `text-[#4848e5] text-sm font-medium` right-aligned with Lock icon
- Button: `h-12 px-5 rounded-lg bg-[#4848e5] hover:bg-[#4848e5]/90 text-white text-base font-bold shadow-sm shadow-[#4848e5]/20`
- Footer: `text-slate-500 text-sm` with "Register here" link in `text-[#4848e5] font-medium`

Keep all existing logic (handleSubmit, error, loading) — only change JSX/classes.
</task>

<task id="4">
### Register Page (match stitch-html/register.html exactly)

Key classes from Stitch source:
- Card: `bg-white rounded-xl shadow-xl shadow-slate-200/50 p-8 border border-slate-100`
- Heading: `text-xl font-bold text-center mb-6`
- Inputs: `h-11 px-4 rounded-lg border-slate-200 text-sm placeholder-slate-400 focus:border-[#4848e5] focus:ring-[#4848e5] focus:ring-1`
- Labels: `text-sm font-medium text-slate-700 mb-1.5`
- Password hint: `mt-1.5 text-xs text-slate-500` "Must be at least 6 characters."
- Button: `h-11 rounded-lg bg-[#4848e5] hover:bg-[#4848e5]/90 text-white font-medium`
- Footer: `mt-6 text-sm text-slate-600` "Already have an account? Sign in"

Keep all existing logic unchanged.
</task>

## must_haves
- Primary `#4848e5` in Tailwind config
- Login/Register visually match Stitch HTML source
- Icon inputs with Mail/Lock Lucide icons
- Brand logo on auth layout
- All auth logic preserved
