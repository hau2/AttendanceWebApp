---
phase: 07-employee-lifecycle-timezone
plan: 05
type: execute
wave: 3
depends_on:
  - 07-03
  - 07-04
files_modified: []
autonomous: false
requirements:
  - EMPL-01
  - EMPL-02
  - EMPL-03
  - EMPL-04
  - TZMG-01
  - TZMG-02

must_haves:
  truths:
    - "Admin can delete an employee; the employee vanishes from user lists; attendance records still show their name"
    - "Admin can edit full name, division, and timezone from the User Management page"
    - "Manager can create a new Employee assigned only to their own divisions"
    - "User table shows a Manager column resolved via the employee's division"
    - "An employee with a personal timezone set gets classified using that timezone — visibly different from company timezone for employees in different zones"
  artifacts:
    - path: "backend/src/database/migrations/008_employee_lifecycle.sql"
      provides: "Migration applied to Supabase"
      contains: "timezone"
    - path: "backend/src/users/users.service.ts"
      provides: "deleteUser, updated updateUser with fullName/timezone"
    - path: "frontend/src/app/(app)/admin/users/components/EditUserModal.tsx"
      provides: "Edit modal with fullName/division/timezone"
    - path: "frontend/src/app/(app)/admin/users/components/UserTable.tsx"
      provides: "Delete button, Edit button, Manager column"
  key_links:
    - from: "Delete button"
      to: "GET /users (refreshed list)"
      via: "handleDelete → deleteUser() API → refreshUsers()"
      pattern: "handleDelete|deleteUser"
    - from: "employee personal timezone"
      to: "check-in classification"
      via: "attendance.service.ts effectiveTimezone override"
      pattern: "effectiveTimezone"
---

<objective>
Human verification checkpoint for Phase 7. All five acceptance criteria for the phase are verified end-to-end through the running application.

Before running: The human must apply migration 008 to Supabase and ensure both backend and frontend servers are running.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Apply migration 008 to Supabase</name>
  <action>
Before verification can begin, apply the Phase 7 database migration:

1. Open Supabase Dashboard → SQL editor
2. Open file: `backend/src/database/migrations/008_employee_lifecycle.sql`
3. Paste and run the migration
4. Confirm: no errors, `users` table now has a `timezone` column

Then start both servers if not already running:
- Backend: `cd backend && npm run start:dev`
- Frontend: `cd frontend && npm run dev`

Visit: http://localhost:3000
  </action>
  <resume-signal>Type "ready" when migration is applied and servers are running</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify all Phase 7 acceptance criteria</name>
  <what-built>
    Employee lifecycle management (delete, edit, manager create) and per-user timezone classification. All six requirements (EMPL-01 through EMPL-04, TZMG-01 and TZMG-02) are implemented across backend and frontend.
  </what-built>
  <how-to-verify>
**Test 1: EMPL-01 — Admin deletes employee, history preserved**
1. Log in as Admin
2. Navigate to /admin/users
3. Find a test Employee with at least one attendance record
4. Click "Delete" → confirm the dialog
5. Verify: employee row disappears from the users list
6. Navigate to /admin/attendance → search for that employee's records
7. Verify: the attendance records still appear with the employee's name showing correctly

**Test 2: EMPL-02 — Admin edits name, division, timezone**
1. As Admin on /admin/users, click "Edit" on any employee
2. Change the Full Name to a different value
3. Change the Division assignment to a different division
4. Enter a timezone: `America/New_York`
5. Click Save Changes
6. Verify: the user row in the table reflects the new name, division, and the edit succeeded without error

**Test 3: EMPL-03 — Manager creates employee in their own division only**
1. Log in as a Manager
2. Navigate to /admin/users (should be accessible, not blocked)
3. Click "Add Employee"
4. Verify: the Division dropdown shows ONLY divisions this Manager manages (not all divisions)
5. Verify: the Role field shows "Employee" (read-only, not a dropdown)
6. Fill in name, email, password, select a division — create the employee
7. Verify: new employee appears in the list assigned to that division

**Test 4: EMPL-04 — Manager column shows responsible manager**
1. As Admin on /admin/users
2. Verify: there is a "Manager" column in the user table
3. For employees assigned to a division that has a manager, the Manager column shows that manager's name
4. For employees with no division or a division with no manager, the column shows "—"

**Test 5: TZMG-01 and TZMG-02 — Per-user timezone classification**
1. As Admin, edit an employee and set their timezone to `America/New_York`
2. Log in as that employee (or use a second browser/incognito window)
3. Attempt check-in — if the employee's local New York time is before/after the shift start by a meaningful amount, the classification should reflect New York time
4. Alternatively: check that PATCH /users/:id with `{"timezone": "America/New_York"}` succeeds (curl or browser network tab) — and that GET /users shows the timezone field on the user object
5. Verify the company timezone is still used for employees with no personal timezone set (check another employee's record)
  </how-to-verify>
  <resume-signal>Type "approved" if all 5 tests pass, or describe any failures</resume-signal>
</task>

</tasks>

<verification>
All 5 test scenarios complete without failures. Human tester confirms each acceptance criterion.
</verification>

<success_criteria>
Phase 7 complete: EMPL-01 through EMPL-04 and TZMG-01 through TZMG-02 all verified working end-to-end in the running application.
</success_criteria>

<output>
After completion, create `.planning/phases/07-employee-lifecycle-timezone/07-05-SUMMARY.md`

Then update STATE.md:
- Current Position: Phase 7 - Employee Lifecycle + Per-User Timezone (COMPLETE)
- Progress: 40% (2/5 v2.0 phases)
- Next: Plan and execute Phase 8 (Remote Work + Acknowledgment Flow)
</output>
