---
phase: 09-advanced-monitoring
plan: 03
type: execute
wave: 3
depends_on:
  - 09-02
files_modified: []
autonomous: false
requirements:
  - RFSH-01
  - RFSH-02
  - RFSH-03
  - RFSH-04
  - FLTR-01
  - FLTR-02
  - FLTR-03
  - FLTR-04
  - FLTR-05

must_haves:
  truths:
    - "Admin can click Data Refresh and see absent + absent_morning rows appear in the table"
    - "Last refresh timestamp updates immediately after refresh completes"
    - "All 5 status filters produce correct filtered results"
    - "Manager sees status filter but NOT the Data Refresh button"
  artifacts:
    - path: "backend/src/database/migrations/011_data_refresh.sql"
      provides: "Applied in Supabase — constraint extended, last_refresh_at column exists"
  key_links: []
---

<objective>
Human verification of all Phase 9 requirements: Data Refresh button (RFSH-01 through RFSH-04) and five attendance status filters (FLTR-01 through FLTR-05).

Purpose: Confirm end-to-end behavior works as specified before marking Phase 9 complete.
Output: Human sign-off on all 9 requirements.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Apply DB migration 011 in Supabase</name>
  <files>backend/src/database/migrations/011_data_refresh.sql</files>
  <action>
Before running the app, apply the DB migration manually. Open the Supabase SQL editor and run the contents of backend/src/database/migrations/011_data_refresh.sql.

This migration:
1. Drops and recreates the check_in_status CHECK constraint to include 'absent' and 'absent_morning'
2. Adds last_refresh_at TIMESTAMPTZ column to companies (nullable — NULL means never run)

Confirm no errors appear in the SQL editor output after running.
  </action>
  <verify>
    <automated>MISSING — manual step; no CLI access to Supabase SQL editor</automated>
  </verify>
  <done>Migration applied successfully in Supabase with no errors.</done>
  <resume-signal>Type "migrated" when migration has been applied successfully in Supabase SQL editor</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: E2E verification — Data Refresh + Status Filters</name>
  <files></files>
  <action>
Start the app (backend + frontend). Verify all 9 Phase 9 requirements end-to-end:

**RFSH-01, RFSH-02, RFSH-03, RFSH-04 — Data Refresh:**
1. Navigate to Admin > Attendance Records page
2. Confirm "Data Refresh" button is visible in the top-right area (admin role only)
3. Click "Data Refresh" — button shows "Refreshing..." while running
4. After completion, verify "Last: [date/time]" appears below the button
5. Verify absent_morning rows now appear for employees who have not checked in today (purple "absent_morning" badge)
6. Verify absent rows appear for employees who had no record at all for yesterday (gray "absent" badge)
7. Click "Data Refresh" a second time — confirm idempotent (no duplicate rows, timestamp updates)
8. Log in as Manager — confirm "Data Refresh" button is NOT visible

**FLTR-01 through FLTR-05 — Status Filters:**
9. Log in as Admin, go to Attendance Records
10. Select "Late" — only red "late" badge records appear
11. Select "Early Leave" — only orange "early" badge records appear
12. Select "Absent" — only gray "absent" records appear
13. Select "Absent Morning" — only purple "absent_morning" records appear
14. Select "Absent Afternoon" — only records with check-in but no checkout appear

**Filter composition + Manager:**
15. Apply "Late" filter + type a name — only records matching both appear
16. Click "Clear filters" — all records return
17. Log in as Manager — status filter dropdown is visible; Data Refresh button is not
  </action>
  <verify>
    <automated>MISSING — requires running app and visual/interactive confirmation</automated>
  </verify>
  <done>Human confirms all 9 requirements (RFSH-01 through RFSH-04, FLTR-01 through FLTR-05) pass end-to-end with no code changes required.</done>
  <resume-signal>Type "approved" if all scenarios pass, or describe any issues found</resume-signal>
</task>

</tasks>

<verification>
All 9 Phase 9 requirements verified by human:
- RFSH-01: Data Refresh button triggers job (admin/owner only)
- RFSH-02: Absent Morning rows created for today's no-checkin employees
- RFSH-03: Absent rows created for yesterday's fully absent employees
- RFSH-04: Last refresh timestamp visible and updates after each run
- FLTR-01: Late filter shows only late records
- FLTR-02: Early Leave filter shows only early checkout records
- FLTR-03: Absent filter shows only absent records
- FLTR-04: Absent Morning filter shows only absent_morning records
- FLTR-05: Absent Afternoon filter shows only records with check-in and no checkout
</verification>

<success_criteria>
Human has confirmed all 9 scenarios pass with no code changes required. Phase 9 is complete.
</success_criteria>

<output>
After completion, create `.planning/phases/09-advanced-monitoring/09-03-SUMMARY.md` documenting verification results. Update STATE.md: phase 9 complete, next = Phase 10 UI Polish.
</output>
