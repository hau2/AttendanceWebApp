---
phase: 11-ip-restriction
plan: 04
type: execute
wave: 3
depends_on:
  - 11-02
  - 11-03
files_modified: []
autonomous: false
requirements:
  - IPRX-01
  - IPRX-02
  - IPRX-03
  - IPRX-04
  - IPRX-05

must_haves:
  truths:
    - "Admin can configure IP mode and it persists"
    - "Admin can add and delete allowlist entries with CIDR and labels"
    - "enforce-block prevents check-in from non-allowlisted IP"
    - "is_remote bypasses enforce-block"
    - "log-only shows warning and allows continuing; record flagged with ip_violation"
    - "disabled mode has no effect"
    - "CIDR range matching works (not just exact IP)"
    - "empty allowlist means no restriction"
  artifacts:
    - path: "frontend/src/app/(app)/admin/settings/page.tsx"
      provides: "Settings page rendering correctly in browser"
    - path: "frontend/src/app/(app)/dashboard/components/CheckInOutCard.tsx"
      provides: "IP pre-check before camera open"
  key_links:
    - from: "Admin settings page"
      to: "backend PATCH /company/settings + POST/DELETE /company/ip-allowlist"
      via: "browser interaction → API calls"
      pattern: "manual verification"
---

<objective>
Human verification of all 5 IPRX requirements end-to-end. Run the backend and frontend, apply the SQL migration, then manually test all IP enforcement scenarios.

Purpose: Confirm Phase 11 is complete and all requirements are met before marking the phase done.
Output: Phase 11 marked complete.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-ip-restriction/11-01-SUMMARY.md
@.planning/phases/11-ip-restriction/11-02-SUMMARY.md
@.planning/phases/11-ip-restriction/11-03-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Apply SQL migration 013</name>
  <instructions>
Run the migration in Supabase SQL editor before testing:

1. Open Supabase Dashboard → SQL Editor
2. Copy and run the contents of `backend/src/database/migrations/013_ip_restriction.sql`
3. Verify in Table Editor that:
   - `companies.ip_mode` CHECK constraint now includes 'disabled'
   - `attendance_records` has a new `ip_violation` column (BOOLEAN, default false)
   - `companies.ip_allowlist` is now JSONB type (not TEXT[])

Once the migration is applied, type "migration applied" to continue.
  </instructions>
  <resume-signal>Type "migration applied" when done</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify all IPRX scenarios</name>
  <what-built>
    - Admin Company Settings page at /admin/settings with IP mode selector and allowlist CRUD
    - IP pre-check in CheckInOutCard before camera opens
    - Backend enforce-block, log-only, disabled modes with CIDR support and remote bypass
    - ip_violation flag on attendance records
  </what-built>
  <how-to-verify>
Run the app (`cd backend && npm run start:dev` + `cd frontend && npm run dev`) and test these scenarios:

**IPRX-01 — IP Mode Configuration:**
1. Log in as Admin → navigate to top nav "Settings" → /admin/settings loads
2. Select "Enforce Block" → click "Save Mode" → "Saved" appears → refresh page → mode is still "Enforce Block"
3. Select "Disabled" → Save → confirm it persists on refresh

**IPRX-02 — Allowlist Management:**
4. On /admin/settings, enter `192.168.1.0/24` in the CIDR field, enter "Office subnet" in Label → click Add → entry appears in list
5. Enter `10.0.0.1` (no label) → Add → second entry appears
6. Click Delete next to `10.0.0.1` → entry disappears from list
7. Empty list state shows "No entries — allowlist is empty"

**IPRX-05 — Empty Allowlist = No Restriction:**
8. With allowlist empty and mode = "Enforce Block": log in as Employee → tap Check-In → camera opens immediately with no blocking error

**IPRX-03 — Enforce Block:**
9. Add an entry like `192.0.2.0/24` (an IP range your machine is definitely NOT on)
10. Set mode to "Enforce Block" → Save
11. Log in as Employee → tap Check-In: "Checking network access..." appears then "Check-in Blocked" error card with your IP shown
12. No camera opens
13. Tick "Remote Work" → tap Check-In again → camera opens (remote bypass works)

**IPRX-04 — Log Only:**
14. Change mode to "Log Only" → Save
15. Log in as Employee (non-matching IP) → tap Check-In → "Outside Office Network" warning appears with "Continue anyway" button
16. Click "Continue anyway" → camera opens normally → take photo → submit → check-in succeeds
17. In Supabase Table Editor, confirm `attendance_records` row has `ip_violation = true`

**IPRX-01 — Disabled Mode:**
18. Change mode to "Disabled" → Save
19. Employee check-in with non-matching IP → camera opens immediately, no warning

**CIDR Test (if accessible via curl):**
20. Run: `curl -s -X GET http://localhost:3001/attendance/ip-check -H "Authorization: Bearer {token}"` → should return `{ ip, withinAllowlist, ipMode }`
  </how-to-verify>
  <resume-signal>Type "verified" if all scenarios pass, or describe which steps failed</resume-signal>
</task>

</tasks>

<verification>
All 5 IPRX requirements confirmed by manual E2E test:
- IPRX-01: Mode persists after save and page refresh
- IPRX-02: Add/delete allowlist entries work with CIDR and labels
- IPRX-03: Enforce-block prevents check-in; remote work bypasses
- IPRX-04: Log-only shows warning; allows continuing; ip_violation=true in DB
- IPRX-05: Empty allowlist = no restriction; CIDR matching works
</verification>

<success_criteria>
All 20 verification steps above pass without errors. Phase 11 is complete.
</success_criteria>

<output>
After completion, create `.planning/phases/11-ip-restriction/11-04-SUMMARY.md`
</output>
