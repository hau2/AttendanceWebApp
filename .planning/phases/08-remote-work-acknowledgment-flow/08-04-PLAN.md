---
phase: 08-remote-work-acknowledgment-flow
plan: 04
type: execute
wave: 4
depends_on: [08-03]
files_modified: []
autonomous: false
requirements: [RMOT-01, RMOT-02, ACKN-01, ACKN-02, ACKN-03, ACKN-04, ACKN-05]

must_haves:
  truths:
    - "Employee can select Remote Work at check-in and the record shows Remote badge"
    - "Manager can Acknowledge a late/early record and the button is replaced by a timestamp"
    - "Manager can Acknowledge a Remote Work record and the button is replaced by a timestamp"
    - "Employee sees acknowledgment status in their history"
  artifacts:
    - path: "backend/src/database/migrations/010_remote_acknowledgment.sql"
      provides: "Migration already applied to Supabase"
    - path: "frontend/src/app/(app)/dashboard/components/CheckInOutCard.tsx"
      provides: "Remote Work toggle functional"
    - path: "frontend/src/app/(app)/admin/attendance/components/AttendanceRecordDetail.tsx"
      provides: "Acknowledge buttons working end-to-end"
  key_links:
    - from: "CheckInOutCard Remote toggle"
      to: "attendance_records.is_remote column"
      via: "POST /attendance/check-in with is_remote: true"
      pattern: "is_remote"
    - from: "AttendanceRecordDetail Acknowledge button"
      to: "attendance_records.acknowledged_at column"
      via: "POST /attendance/records/:id/acknowledge"
      pattern: "acknowledged_at"
---

<objective>
Human verification of all Phase 8 features: Remote Work check-in, Remote badge in views, and Manager Acknowledge flow.

Purpose: Confirm the full end-to-end product experience works correctly before closing the phase.
Output: Phase 8 approved or issues reported for correction.
</objective>

<execution_context>
@./.claude/agents/gsd-executor.md
@./.claude/agents/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
Phase 8 complete:
- SQL migration 010_remote_acknowledgment.sql (5 new columns on attendance_records)
- Backend: CheckInDto accepts is_remote; POST /attendance/records/:id/acknowledge; POST /attendance/records/:id/acknowledge-remote
- Frontend: Remote Work checkbox on check-in, Remote badge in all views, Manager Acknowledge buttons, Employee history acknowledgment status

BEFORE TESTING: Run backend/src/database/migrations/010_remote_acknowledgment.sql in Supabase SQL editor to add the new columns.
Then: cd backend && npm run start:dev and cd frontend && npm run dev
  </what-built>
  <how-to-verify>
Test these scenarios in order:

**Scenario 1 — Remote Work check-in (RMOT-01, RMOT-02):**
1. Log in as an Employee account
2. Go to Dashboard — confirm "Working remotely today" checkbox appears below the date, above the CHECK IN button
3. Tick the checkbox, then click CHECK IN and complete the photo capture + submit
4. The record should appear in the employee's history with a blue "Remote" badge

**Scenario 2 — Remote badge in admin view (RMOT-02):**
5. Log in as Admin
6. Go to /admin/attendance — the remote check-in record should show a blue "Remote" badge in the table's Remote column

**Scenario 3 — Late reason visible for Manager (ACKN-01):**
7. Log in as an Employee, check in late (don't use remote), provide a late reason
8. Log in as Manager
9. Go to /admin/attendance — confirm the late reason text appears in the "Notes" column for that record

**Scenario 4 — Manager Acknowledge late/early (ACKN-02, ACKN-03):**
10. As Manager, click "View" on the late record opened in step 7
11. In the detail modal, confirm "Acknowledge Late/Early" button is visible (amber button)
12. Click it — button should disappear and be replaced by a green "Acknowledged [timestamp]" text
13. Log in as the Employee, go to /attendance/history — confirm the "Acknowledged" column shows the date for that record (ACKN-03)

**Scenario 5 — Manager Acknowledge Remote (ACKN-04, ACKN-05):**
14. Log in as Manager
15. Click "View" on the Remote Work record from Scenario 1
16. Confirm "Acknowledge Remote" button (blue button) is visible alongside or after the Remote badge
17. Click it — button should disappear and be replaced by "Remote acknowledged [timestamp]"
18. Log in as Employee, go to /attendance/history — confirm "Acknowledged" column shows "Remote: [date]" for that record (ACKN-05)

**Scenario 6 — Non-remote, non-late record (guard check):**
19. Log in as Employee who has an on-time check-in with no remote flag
20. As Manager, view that record in detail — confirm NO Acknowledge buttons are visible (record has nothing to acknowledge)

**Scenario 7 — Admin view (not manager):**
21. Log in as Admin, open any record detail — Acknowledge buttons should still appear (admin/owner are included in canAcknowledge)
  </how-to-verify>
  <resume-signal>Type "approved" if all 7 scenarios pass, or describe any issues found</resume-signal>
</task>

</tasks>

<verification>
All 7 verification scenarios pass with no unexpected behavior.
</verification>

<success_criteria>
Phase 8 human-approved. All 7 requirements (RMOT-01, RMOT-02, ACKN-01 through ACKN-05) confirmed working end-to-end.
</success_criteria>

<output>
After human approval, create `.planning/phases/08-remote-work-acknowledgment-flow/08-04-SUMMARY.md`
</output>
