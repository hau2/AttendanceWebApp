---
phase: 12-ui-polish
plan: 05
type: execute
wave: 3
depends_on:
  - 12-02
  - 12-03
  - 12-04
files_modified: []
autonomous: false
requirements:
  - UIUX-01
  - UIUX-02
  - UIUX-03
  - UIUX-04
  - UIUX-05

must_haves:
  truths:
    - "Live clock ticks every second on Employee Home without any page reload"
    - "All status badges carry a Lucide icon in every view"
    - "Executive can drill down to any employee's history via the ranking table"
    - "Manager Employee Detail page is accessible and shows complete monthly attendance inline"
    - "New modal and table surfaces (EmployeeHistoryModal, Employee Detail page) use Shadcn components"
  artifacts:
    - path: "frontend/src/components/ui/status-badge.tsx"
      provides: "Shared StatusBadge + RemoteBadge used everywhere"
    - path: "frontend/src/app/(app)/executive/components/EmployeeHistoryModal.tsx"
      provides: "Executive drill-down using Shadcn Dialog + Table"
    - path: "frontend/src/app/(app)/admin/employees/[id]/page.tsx"
      provides: "Manager Employee Detail page using Shadcn Table"
  key_links:
    - from: "Employee Home"
      to: "clock state"
      via: "setInterval updating HH:MM:SS every second"
      pattern: "clock ticks visually"
    - from: "Executive Late Ranking row"
      to: "EmployeeHistoryModal"
      via: "onClick -> selectedEmployee state"
      pattern: "drill-down opens"
    - from: "Attendance table employee name"
      to: "/admin/employees/[id]"
      via: "Next.js Link"
      pattern: "navigates to detail page"
---

<objective>
Human E2E verification of all five UIUX requirements. The executor guides the user through a structured test script covering live clock, status badge icons, executive drill-down, manager detail page, and Shadcn component usage.

Purpose: Confirm every UIUX requirement works correctly in the live environment before marking Phase 12 complete.
Output: VERIFIED or list of issues to fix
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Start dev server and confirm build passes</name>
  <files></files>
  <action>
    From `frontend/` directory, run:
    ```
    npm run build 2>&1 | tail -20
    ```

    The build must complete without TypeScript errors or missing module errors. If the build fails:
    - Missing Shadcn component imports → run the missing `npx shadcn@latest add {component}` command
    - TypeScript errors → fix in the relevant file before proceeding

    Then start the dev server in background for human verification:
    ```
    npm run dev
    ```
  </action>
  <verify>
    <automated>cd /Users/avepoint_admin/Downloads/AttendanceWebApp/frontend && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>Build completes with "✓ Compiled successfully" or equivalent; no TypeScript errors</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Phase 12 UI Polish complete:
    - UIUX-01: Live HH:MM:SS clock on Employee Home
    - UIUX-02: Lucide status badge icons in every attendance view
    - UIUX-03: Shadcn Dialog + Table in new v2.0 UI surfaces
    - UIUX-04: Executive employee history drill-down modal
    - UIUX-05: Manager Employee Detail page at /admin/employees/[id]
  </what-built>
  <how-to-verify>
    Start backend (cd backend && npm run start:dev) and frontend (cd frontend && npm run dev).
    Then test each requirement:

    **UIUX-01 — Live Clock:**
    1. Log in as Employee (or Manager with a shift assigned)
    2. Navigate to Dashboard (/dashboard)
    3. Verify: A HH:MM:SS clock is visible below the date
    4. Verify: The seconds digit ticks forward every second without any page action
    5. Verify: The time matches your device's local time

    **UIUX-02 — Status Badge Icons:**
    6. Log in as Admin
    7. Navigate to Attendance Records (/admin/attendance)
    8. Verify: Status badges in the table (Late, On Time, Early Leave, Absent, etc.) each show a small icon to the left of the text
    9. Click "View" on any record with a Late or On-time status → open AttendanceRecordDetail modal
    10. Verify: Status badges in the modal also show icons
    11. Log in as Employee → navigate to Attendance History (/attendance/history)
    12. Verify: Status badges in the history table show icons
    13. Verify: Remote badge shows a Laptop icon

    **UIUX-03 — Shadcn Components:**
    14. Log in as Executive (/executive)
    15. Click any employee row in the Late Frequency Ranking
    16. Verify: A modal opens (styled differently from the old plain Tailwind modals — Shadcn Dialog has a backdrop and centered card)
    17. Navigate to /admin/employees/{any-user-id}
    18. Verify: The table in that page has the Shadcn table style (bordered rows, header background)

    **UIUX-04 — Executive Drill-down:**
    19. Log in as Executive (/executive)
    20. Select a month with records
    21. Click an employee row in the Late Frequency Ranking table
    22. Verify: Modal opens showing that employee's full attendance history for the selected month
    23. Verify: No edit controls, adjust buttons, or acknowledge buttons appear — read-only
    24. Close the modal (click X or press Escape)

    **UIUX-05 — Manager Employee Detail Page:**
    25. Log in as Manager (/admin/attendance)
    26. Locate a record for one of your team's employees
    27. Click the employee's name (should be a link now)
    28. Verify: Navigates to /admin/employees/{id} — Employee Detail page
    29. Verify: Page shows the employee's name and a monthly attendance table
    30. Verify: Table columns include: Date, Check-in time, In Status (with icon), Check-out, Out Status (with icon), Remote, Notes (late reason / early note inline), Acknowledged
    31. Verify: Month navigation arrows let you browse to previous/next month
  </how-to-verify>
  <resume-signal>Type "verified" if all 31 steps pass, or describe any failures so they can be fixed</resume-signal>
</task>

</tasks>

<verification>
All 5 UIUX requirements confirmed by human tester in live environment.
</verification>

<success_criteria>
Human tester confirms:
1. Live clock ticks every second (UIUX-01)
2. All status badges show Lucide icons (UIUX-02)
3. Executive drill-down opens Shadcn Dialog with Table (UIUX-03 + UIUX-04)
4. Manager Employee Detail page accessible and shows inline notes + ack status (UIUX-03 + UIUX-05)
</success_criteria>

<output>
After verification passes, create `.planning/phases/12-ui-polish/12-05-SUMMARY.md`
Update STATE.md: Phase 12 complete.
Update ROADMAP.md: mark Phase 12 checkbox as [x].
</output>
