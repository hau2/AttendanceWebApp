---
phase: 12-ui-polish
plan: 02
type: execute
wave: 2
depends_on:
  - 12-01
files_modified:
  - frontend/src/app/(app)/dashboard/page.tsx
  - frontend/src/app/(app)/attendance/history/components/AttendanceHistoryTable.tsx
  - frontend/src/app/(app)/admin/attendance/components/AttendanceRecordTable.tsx
  - frontend/src/app/(app)/admin/attendance/components/AttendanceRecordDetail.tsx
autonomous: true
requirements:
  - UIUX-01
  - UIUX-02

must_haves:
  truths:
    - "Employee Home page shows a live HH:MM:SS clock that ticks every second"
    - "Clock displays device local time without any page reload"
    - "Every status badge in AttendanceHistoryTable uses StatusBadge with a Lucide icon"
    - "Every status badge in AttendanceRecordTable uses StatusBadge with a Lucide icon"
    - "Remote badge in all views uses RemoteBadge from status-badge.tsx"
    - "AttendanceRecordDetail status displays use StatusBadge"
  artifacts:
    - path: "frontend/src/app/(app)/dashboard/page.tsx"
      provides: "Live clock via setInterval in useEffect"
      contains: "setInterval"
    - path: "frontend/src/app/(app)/attendance/history/components/AttendanceHistoryTable.tsx"
      provides: "Employee history table with Lucide status badges"
      contains: "StatusBadge"
    - path: "frontend/src/app/(app)/admin/attendance/components/AttendanceRecordTable.tsx"
      provides: "Admin/Manager attendance table with Lucide status badges"
      contains: "StatusBadge"
    - path: "frontend/src/app/(app)/admin/attendance/components/AttendanceRecordDetail.tsx"
      provides: "Record detail modal with Lucide status badges"
      contains: "StatusBadge"
  key_links:
    - from: "frontend/src/app/(app)/dashboard/page.tsx"
      to: "setInterval / clearInterval"
      via: "useEffect with 1000ms interval"
      pattern: "setInterval.*1000"
    - from: "frontend/src/app/(app)/attendance/history/components/AttendanceHistoryTable.tsx"
      to: "frontend/src/components/ui/status-badge.tsx"
      via: "named import"
      pattern: "from '@/components/ui/status-badge'"
---

<objective>
Add a live ticking clock to the Employee Home page and replace all inline statusBadge functions across existing attendance views with the shared StatusBadge and RemoteBadge components from plan 12-01.

Purpose: Satisfies UIUX-01 (live clock) and completes UIUX-02 (icon badges everywhere) by wiring the shared component into all three existing attendance views.
Output: Modified dashboard page, AttendanceHistoryTable, AttendanceRecordTable, AttendanceRecordDetail
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/12-ui-polish/12-01-SUMMARY.md
</context>

<interfaces>
<!-- From 12-01: StatusBadge and RemoteBadge -->
From frontend/src/components/ui/status-badge.tsx:
```typescript
export function StatusBadge({ status, missingCheckout }: { status: string | null; missingCheckout?: boolean }): JSX.Element | null;
export function RemoteBadge(): JSX.Element;
```

<!-- Current dashboard page structure -->
From frontend/src/app/(app)/dashboard/page.tsx:
- 'use client' component
- Uses useState for user, renders CheckInOutCard
- Currently shows static date via `new Date().toLocaleDateString(...)`
- No clock currently
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Add live HH:MM:SS clock to Employee Home</name>
  <files>frontend/src/app/(app)/dashboard/page.tsx</files>
  <action>
    In `dashboard/page.tsx`:

    1. Add a `clock` state: `const [clock, setClock] = useState('')`

    2. Add a useEffect that sets up a `setInterval` at 1000ms to update the clock string using `new Date().toLocaleTimeString([], { hour: '2-digit', minute: '2-digit', second: '2-digit', hour12: false })`. Capture the current value immediately on mount so the clock shows instantly (no 1s delay). Return `() => clearInterval(id)` to clean up.

    3. In the JSX, add the clock display below the existing date paragraph:
       ```tsx
       {clock && (
         <p className="text-gray-900 text-3xl font-mono font-semibold mt-1 tabular-nums">{clock}</p>
       )}
       ```
       Place this inside the existing `<div className="mb-6">` block, after the existing `<p>` date element.

    The clock must:
    - Start ticking immediately on mount (call the formatter once before setInterval)
    - Use device local timezone (default behavior of toLocaleTimeString with no timeZone option)
    - Display HH:MM:SS format (24h via hour12: false)
    - Clean up interval on unmount to avoid memory leaks
    - Not affect the check-in/out flow or other state
  </action>
  <verify>
    <automated>cd /Users/avepoint_admin/Downloads/AttendanceWebApp/frontend && npx tsc --noEmit 2>&1 | grep "dashboard/page" || echo "PASS - no dashboard/page errors"</automated>
  </verify>
  <done>Dashboard page renders a clock element; TypeScript compiles cleanly; interval cleanup is present</done>
</task>

<task type="auto">
  <name>Task 2: Wire StatusBadge into all attendance views</name>
  <files>
    frontend/src/app/(app)/attendance/history/components/AttendanceHistoryTable.tsx,
    frontend/src/app/(app)/admin/attendance/components/AttendanceRecordTable.tsx,
    frontend/src/app/(app)/admin/attendance/components/AttendanceRecordDetail.tsx
  </files>
  <action>
    In each of the three files, replace the local inline `statusBadge` helper function with the shared `StatusBadge` and `RemoteBadge` components from `@/components/ui/status-badge`.

    **For AttendanceHistoryTable.tsx:**
    - Remove the local `statusBadge(status, missing)` function
    - Add import: `import { StatusBadge, RemoteBadge } from '@/components/ui/status-badge';`
    - Replace all `{statusBadge(r.check_in_status, r.missing_checkout)}` calls with `<StatusBadge status={r.check_in_status} missingCheckout={r.missing_checkout} />`
    - Replace `{r.is_remote && <span ...>Remote</span>}` with `{r.is_remote && <RemoteBadge />}`

    **For AttendanceRecordTable.tsx:**
    - Remove the local `statusBadge(status)` function
    - Add import: `import { StatusBadge, RemoteBadge } from '@/components/ui/status-badge';`
    - Replace all `{statusBadge(r.check_in_status)}` calls with `<StatusBadge status={r.check_in_status} />`
    - Replace `{statusBadge(r.check_out_status)}` with `<StatusBadge status={r.check_out_status} />`
    - The "Missing checkout" span in check_out_status column: replace with `<StatusBadge status={null} missingCheckout={r.missing_checkout} />` guarded by `r.missing_checkout`
    - Replace `{r.is_remote && <span ...>Remote</span>}` with `{r.is_remote && <RemoteBadge />}`

    **For AttendanceRecordDetail.tsx:**
    - Remove the local `statusBadge(status)` function
    - Add import: `import { StatusBadge, RemoteBadge } from '@/components/ui/status-badge';`
    - Replace `{statusBadge(record.check_in_status)}` with `<StatusBadge status={record.check_in_status} />`
    - Replace `{statusBadge(record.check_out_status)}` with `<StatusBadge status={record.check_out_status} />`
    - Replace the inline Remote Work badge span with `<RemoteBadge />`

    After all replacements verify no references to the old local `statusBadge` function remain in these three files.
  </action>
  <verify>
    <automated>cd /Users/avepoint_admin/Downloads/AttendanceWebApp/frontend && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>All three files import StatusBadge/RemoteBadge; no local statusBadge functions remain; TypeScript compiles without errors</done>
</task>

</tasks>

<verification>
- `grep -n "setInterval" frontend/src/app/(app)/dashboard/page.tsx` shows interval setup
- `grep -n "clearInterval" frontend/src/app/(app)/dashboard/page.tsx` shows cleanup
- `grep -rn "from '@/components/ui/status-badge'" frontend/src/app/` shows all three views importing
- `grep -n "function statusBadge" frontend/src/app/(app)/attendance/history/components/AttendanceHistoryTable.tsx` returns nothing (removed)
- `npx tsc --noEmit` passes in frontend/
</verification>

<success_criteria>
1. Live clock ticks every second on Employee Home without page reload (UIUX-01)
2. All status badges in history, admin records table, and record detail show a Lucide icon alongside text (UIUX-02)
3. Remote badge uses Laptop icon via RemoteBadge in all three views
</success_criteria>

<output>
After completion, create `.planning/phases/12-ui-polish/12-02-SUMMARY.md`
</output>
